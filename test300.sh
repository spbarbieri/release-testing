#!/bin/bash
# This script runs the ls command with the -alh options
# The -a option lists all files and directories, including hidden ones
# The -l option formats the output in a long list format
# The -h option formats the sizes of files and directories in a human-readable format

ls -alh